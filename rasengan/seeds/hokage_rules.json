{
  "rules": [
    {
      "name": "hokage_block_tracker",
      "description": "Tracks all Hokage BLOCK events for weekly review. Logs which edicts fire most frequently to identify patterns and potential false positives.",
      "event_pattern": "hokage.blocked",
      "conditions": [],
      "actions": [
        {
          "type": "log",
          "config": {
            "message": "Hokage blocked command: {payload.reason}"
          }
        }
      ],
      "cooldown_seconds": 0,
      "enabled": true
    },
    {
      "name": "hokage_warn_tracker",
      "description": "Tracks Hokage WARN events for pattern analysis.",
      "event_pattern": "hokage.warned",
      "conditions": [],
      "actions": [
        {
          "type": "log",
          "config": {
            "message": "Hokage warned on command: {payload.reason}"
          }
        }
      ],
      "cooldown_seconds": 0,
      "enabled": true
    }
  ],
  "schedules": [
    {
      "name": "hokage_weekly_review",
      "description": "Weekly Hokage enforcement review — emits event to trigger edict audit",
      "cron": "0 20 * * 0",
      "event_type": "hokage.weekly_review",
      "payload": {
        "task": "Review enforcement log, identify most-triggered edicts, check for false positives, suggest new edicts from recent mistakes"
      },
      "enabled": true
    }
  ]
}
