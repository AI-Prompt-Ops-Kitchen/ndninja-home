# Sage Mode Production Environment Configuration
# Copy to .env and configure for your deployment

# === REQUIRED - Must be set ===

# Database credentials
POSTGRES_USER=sage_user
POSTGRES_PASSWORD=CHANGE_THIS_SECURE_PASSWORD
POSTGRES_DB=sage_mode

# JWT secret - Generate with: python -c "import secrets; print(secrets.token_urlsafe(64))"
JWT_SECRET_KEY=CHANGE_THIS_GENERATE_SECURE_KEY

# === OPTIONAL - Defaults shown ===

# JWT Configuration
JWT_ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=15
REFRESH_TOKEN_EXPIRE_DAYS=7

# CORS origins (comma-separated)
CORS_ORIGINS=https://yourdomain.com

# Logging
LOG_LEVEL=INFO

# Anthropic API key (required for LLM-powered agents)
ANTHROPIC_API_KEY=sk-ant-...

# === SSL Certificates ===
# Place your SSL certificates in nginx/ssl/
# - nginx/ssl/cert.pem
# - nginx/ssl/key.pem
